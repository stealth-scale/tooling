/**
 * @fileoverview Names the directory a repository keeps its Storybook configuration in, which
 * is Storybook's own default. The configuration and the test project both resolve against it,
 * so the two read one name.
 */

/**
 * Names the directory Storybook reads a repository's configuration from, relative to the
 * repository root.
 */
export const CONFIG_DIR = '.storybook'
