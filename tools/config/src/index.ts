/**
 * @fileoverview Holds the toolchain configuration every stealth repository runs: one builder
 * per block of a vite-plus config, re-exported from `src/vite/`.
 */

export * from './vite/index.ts'
