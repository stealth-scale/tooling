---
'@stealthscale/tool-storybook': patch
---

Gives a variant matrix room between its rows. The vertical gap doubles and the horizontal gap widens, both as multiples of the theme's own spacing, so a row of controls reads as a row rather than as one block.
